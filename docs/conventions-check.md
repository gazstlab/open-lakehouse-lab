# Conventions Check

This file exists to trigger and validate the initial GitHub Actions conventions workflow.

The related pull request should use the required title format:

```text
<type>(optional-scope): <summary> (#<issue-number>)
```

Example:

```text
ci(github): test convention workflow (#1)
```
