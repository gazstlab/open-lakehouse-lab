# Conventions Check v2

This file exists to trigger a new Pull Request after updating the Security workflow to use a valid Trivy action version.

Expected PR title format:

```text
ci(github): retest convention workflow (#1)
```
