---
extends: default

rules:
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values: ["true", "false", "on", "off"]
  comments:
    min-spaces-from-content: 1

ignore: |
  .venv/
  venv/
  target/
  .ruff_cache/
  node_modules/
